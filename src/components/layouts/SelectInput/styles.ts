import styled from 'styled-components';

export const Container = styled.div`
    > select {
        padding: 7px 10px;
        margin-right: 20px;
        border-radius: 5px;
        color: #000;
    }

    > select > option {
        color: #000;
    }

`